# # Local testing only — points the frontend at a locally-served Laravel API.
# # Restart `npm run dev` after changing. Delete this file to revert to the default.
# VITE_API_BASE_URL=http://127.0.0.1:8000/api

# # Uploaded files (photos, certificates, agreements) live on the server's storage,
# # so load them from live even while the API is local.
# VITE_ASSET_BASE_URL=https://teamdev.seewework.com
